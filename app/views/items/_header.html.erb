<header class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header"> <h3><%= link_to "GrowCart", items_path , id: "logo" %></h3></div>
    <p class="navbar-text navbar-right">Signed in as <%= @userName.nil? ? "Not signed in" : @userName.name %></p>
    <p class="navbar-text navbar-right"> <%= @userName.nil? ? link_to("login" , login_path) : link_to("Logout" , logout_path , method: :delete) %> </p>

  </div>
</header>