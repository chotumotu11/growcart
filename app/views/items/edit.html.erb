<%= form_for(@my_item) do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<br>
<%= f.label :price %>
<%= f.text_field :price %>
<br>
<%= f.label :description %>
<%= f.text_area :description %>
<br>
<div class=field>
	<%= f.label :category %>
	<%= select_tag(:category , options_from_collection_for_select( Category.all , :id , :name, @my_item.getcategory[:id] ) , { class: "auto-update"} ) %>
</div>
<div class=field>
		<%= f.label :subcategory %>
	<%= select_tag(:subcategory , options_from_collection_for_select( @my_item.getcategory.subcategories.all , :id , :name, @my_item.getsubcategory[:id] ) , { class: "sub-update"} ) %>
</div>
<div class=field>
	<%= f.label :brand %>
	<%= select_tag(:brand , options_from_collection_for_select( @my_item.getsubcategory.brands.all , :id , :name, @my_item.getbrand[:id] ) , { class: "brand"} ) %>
</div>
<br>
<%= f.submit class: "btn btn-success" %>
<% end %>